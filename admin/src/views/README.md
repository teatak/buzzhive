# Views

Page-level route components live here.

Current planned split:

- `DashboardView`
- `MyApiKeysView`
- `UsersView`
- `GoogleAccountsView`
- `RuntimeView`

Views receive data/actions from `App` until the app grows enough to justify feature stores.
